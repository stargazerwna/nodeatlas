{
  "name": "netmonitor",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "main": "electron/main.js",
  "scripts": {
    "dev": "vite",
    "server": "node server.js",
    "build": "vite build",
    "preview": "vite preview",
    "electron": "npm run build && electron .",
    "package:win": "npm run build && electron-builder --win portable"
  },
  "dependencies": {
    "net-snmp": "^3.26.0"
  },
  "devDependencies": {
    "electron": "^37.2.1",
    "electron-builder": "^26.0.0",
    "vite": "^7.0.0"
  },
  "build": {
    "appId": "com.netmonitor.nodeatlas",
    "productName": "NodeAtlas",
    "icon": "build/icon.ico",
    "directories": {
      "output": "dist-electron"
    },
    "files": [
      "dist/**/*",
      "build/**/*",
      "server.js",
      "nodes.example.json",
      "nodes.local.json",
      "links.example.json",
      "links.local.json",
      "workspace.local.json",
      "electron/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        "portable"
      ],
      "artifactName": "NodeAtlas-${version}-portable.exe"
    }
  },
  "description": "NodeAtlas desktop monitoring app",
  "author": {
    "name": "NetMonitor",
    "email": "support@example.com"
  }
}
